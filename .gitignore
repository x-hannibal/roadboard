# Windows
Desktop.ini

# Roadboard 2.0 reference — gitignored, analyzed locally as sample only
/RB.v2/

# Workflow artifacts (task queue + ephemeral agent scratchpad)
/tasks/
/.agent/

# Local env / secrets
.env
.env.local
.env.*.local

# Rust build output
/target/

# SQLx offline query data goes to version control (sqlx convention).
# Ignoring the legacy sqlx-data.json path only.
sqlx-data.json

# Rust backup files generated by older editors
**/*.rs.bk

# Node / Next.js
/apps/web/node_modules/
/apps/web/.next/
/apps/web/out/

# Tauri (apps/desktop lands in M5)
/apps/desktop/target/
/apps/desktop/src-tauri/target/

# Editor / OS
.DS_Store
Thumbs.db
